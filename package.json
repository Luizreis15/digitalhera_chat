{
  "name": "herachat-landing",
  "version": "1.0.0",
  "private": true,
  "description": "HeraChat — Landing page institucional (site estático)",
  "scripts": {
    "dev": "npx serve . -p 3000 --cors",
    "start": "npx serve . -p 3000",
    "build": "echo 'Site estático — sem build necessário'"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
